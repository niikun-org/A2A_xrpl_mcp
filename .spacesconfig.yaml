# HuggingFace Spaces Configuration
# This file is optional but can help configure the Space

# Specify the Space SDK
sdk: gradio
sdk_version: "4.0.0"

# App file (entry point)
app_file: app.py

# Python version
python_version: "3.10"

# Emoji for the Space
emoji: 🔐

# Color scheme
colorFrom: blue
colorTo: purple

# Title
title: A2A Trace Anchoring - MCP Demo

# Short description
short_description: Tamper-proof AI agent logging with blockchain anchoring

# Pinned (featured)
pinned: false

# License
license: apache-2.0

# Disable hardware acceleration (not needed)
disable_embedding: false
